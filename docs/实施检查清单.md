# SURS 架构重构实施检查清单

## ✅ 阶段一：核心项目创建（优先级：高）

### 1.1 创建 SURS.Core 项目
- [ ] 创建项目文件 `SURS.Core/SURS.Core.csproj`
- [ ] 配置项目引用关系
- [ ] 创建文件夹结构（Domain, Services, Business, Common）

### 1.2 拆分领域模型
- [ ] 创建 `Domain/Entities/Report/` 模块
- [ ] 创建 `Domain/Entities/Uterus/` 模块
- [ ] 创建 `Domain/Entities/Endometrium/` 模块
- [ ] 创建 `Domain/Entities/Adnexa/` 模块
- [ ] 创建 `Domain/Entities/Fluid/` 模块
- [ ] 创建 `Domain/ValueObjects/` 模块
- [ ] 创建 `Domain/Enums/` 模块

### 1.3 定义服务接口
- [ ] 创建 `IORadsCalculator` 接口
- [ ] 创建 `IPdfGenerator` 接口
- [ ] 创建 `IReportValidator` 接口
- [ ] 创建 `IReportRepository` 接口

### 1.4 迁移业务逻辑
- [ ] 迁移 `ORadsCalculator` 到 `Business` 层
- [ ] 创建 `ReportValidator` 类
- [ ] 创建 `ReportTextGenerator` 类（从 PdfService 提取）

---

## ✅ 阶段二：基础设施层（优先级：高）

### 2.1 创建 SURS.Infrastructure 项目
- [ ] 创建项目文件
- [ ] 添加对 SURS.Core 的引用
- [ ] 添加 QuestPDF NuGet 包

### 2.2 实现服务
- [ ] 实现 `PdfService`（实现 `IPdfGenerator`）
- [ ] 创建 `FileReportRepository`（实现 `IReportRepository`）
- [ ] 创建 `ConfigurationService`
- [ ] 创建 `FileLogger`（实现 `ILogger`）

### 2.3 文件持久化
- [ ] 实现 JSON 序列化/反序列化
- [ ] 实现文件存储服务
- [ ] 添加报告历史记录功能

---

## ✅ 阶段三：App 层重构（优先级：中）

### 3.1 依赖注入
- [ ] 添加 `Microsoft.Extensions.DependencyInjection` 包
- [ ] 配置服务容器
- [ ] 更新 MainWindow 使用 DI

### 3.2 ViewModel 拆分
- [ ] 创建 `BaseViewModel`
- [ ] 拆分 `MainViewModel`
- [ ] 创建 `ReportHeaderViewModel`
- [ ] 创建 `UterusViewModel`
- [ ] 创建 `EndometriumViewModel`
- [ ] 创建 `AdnexaViewModel`
- [ ] 创建 `PreviewViewModel`

### 3.3 对话框服务
- [ ] 创建 `IDialogService` 接口
- [ ] 实现 `DialogService`
- [ ] 更新 ViewModel 使用对话框服务

---

## ✅ 阶段四：优化与测试（优先级：中）

### 4.1 性能优化
- [ ] 实现预览刷新节流（300ms）
- [ ] 优化事件订阅管理
- [ ] 使用 WeakEventManager
- [ ] 异步操作优化

### 4.2 验证框架
- [ ] 实现 `IReportValidator`
- [ ] 添加字段验证规则
- [ ] 添加业务规则验证
- [ ] UI 显示验证错误

### 4.3 测试
- [ ] 创建测试项目
- [ ] 编写 ORadsCalculator 测试
- [ ] 编写 ReportValidator 测试
- [ ] 编写 ViewModel 测试（使用 Mock）

---

## ✅ 阶段五：配置与日志（优先级：低）

### 5.1 配置管理
- [ ] 创建 `appsettings.json`
- [ ] 实现 `IConfigurationService`
- [ ] 保存用户偏好设置
- [ ] 加载应用配置

### 5.2 日志系统
- [ ] 实现 `ILogger` 接口
- [ ] 实现 `FileLogger`
- [ ] 添加全局异常处理
- [ ] 记录关键操作日志

---

## 📊 进度跟踪

- **阶段一**: 0% (待开始)
- **阶段二**: 0% (待开始)
- **阶段三**: 0% (待开始)
- **阶段四**: 0% (待开始)
- **阶段五**: 0% (待开始)

**总体进度**: 0%

---

## 🎯 下一步行动

1. **立即开始**: 创建 SURS.Core 项目
2. **本周完成**: 阶段一和阶段二的基础部分
3. **下周完成**: 阶段三的依赖注入和 ViewModel 拆分
4. **持续优化**: 阶段四和阶段五可以并行进行

